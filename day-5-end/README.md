
### Topics in this Project

#### 1. Using regular CSS
### Using regular CSS, we can just add a css sheet and import it and it will work

#### 2. Using regular SCSS , What we need is to install ``sass`` so all we do is ``npm install sass``
## reference : https://create-react-app.dev/docs/adding-a-sass-stylesheet/
## Adding partials _typography.scss _layout.scss _base.scss
